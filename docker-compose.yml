version: '3.8'

services:
  backend:
    container_name: llanteria-sv2-backend
    build: ./backend-laravel
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    environment:
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-3306}
      - DB_DATABASE=${DB_NAME:-llanteria}
      - DB_USERNAME=${DB_USER:-caleth}
      - DB_PASSWORD=${DB_PASSWORD:-calethpass}
      - APP_ENV=${APP_ENV:-local}
      - APP_KEY=${APP_KEY:-}
      - APP_URL=${APP_URL:-http://localhost:3000}
    working_dir: /var/www/html
    command: sh -c "chmod +x start-backend.sh && ./start-backend.sh"
    depends_on:
      - db
    volumes:
      - ./backend-laravel:/var/www/html

  frontend:
    container_name: llanteria-sv2-frontend
    build:
      context: ./frontend-react
      dockerfile: Dockerfile.dev
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://127.0.0.1:3000/api}
    working_dir: /app
    command: npm run dev -- --host 0.0.0.0 --port 5173
    depends_on:
      - backend
    volumes:
      - ./frontend-react:/app
      - /app/node_modules

  db:
    container_name: llanteria-sv2-db
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_NAME:-llanteria}
      MYSQL_USER: ${DB_USER:-caleth}
      MYSQL_PASSWORD: ${DB_PASSWORD:-calethpass}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpass}
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
